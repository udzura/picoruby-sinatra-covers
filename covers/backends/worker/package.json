{
  "name": "picoruby-sinatra-covers-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --log-level debug --port ${WORKER_PORT:-8787}"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
